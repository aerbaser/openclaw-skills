# MCP Servers

Custom Model Context Protocol servers for Sokrat agent.

## Structure
Each server in its own subdirectory:
```
mcp/
  server-name/
    index.js (or main.py)
    README.md
    package.json (if Node)
```

## Configured servers
_None yet. Add servers here and register in openclaw.json → mcpServers._
