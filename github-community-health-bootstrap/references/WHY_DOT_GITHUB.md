# Why `.github`

A public `.github` repository gives one fallback place for default community health files and default issue templates / PR templates across the portfolio.

Use it so every repo does not need to reinvent the same intake and contribution files.
