# Scenario: bootstrap org baseline

User request:
"Set up a sane GitHub operating baseline for my org so agents stop creating chaos."

Expected behavior:
- recommends a public `.github` repo
- recommends template repos
- defines repo baseline and review cadence
- routes new work to issue + CI skills
